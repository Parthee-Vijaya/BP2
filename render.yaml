services:
  - type: web
    name: barnepige-timeregistrering
    runtime: node
    buildCommand: npm install && cd frontend && npm install && npm run build && cd ../backend && npm install && node seed-demo.js
    startCommand: cd backend && node src/index.js
    envVars:
      - key: NODE_ENV
        value: production
